name = "ai-private-bot"
main = "src/index.js"
compatibility_date = "2024-09-23"

# ─── Production (branch: main) ───────────────────────────────────────────────
[vars]
ALLOWED_ORIGIN = "https://giamat13.github.io"

# ─── Preview (branch: test) ──────────────────────────────────────────────────
[env.test]
name = "ai-private-bot-test"
vars = { ALLOWED_ORIGIN = "https://giamat13.github.io" }
